class Fight:
	attackers = {}
	defenders = {}
 
	def addAttacker(thisFighter):
		self.attackers[len(self.attackers)] = thisFighter
  
	def addDefender(thisFighter):
		self.defenders[len(self.defenders)] = thisFighter
  
	def start():
		# initiative ermitteln
		# der reihe nach prügeln
		# events ausgeben
  
	def end():
		# tote sterben lassen
		# loot abwerfen